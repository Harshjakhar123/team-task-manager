# Frontend environment variables (optional).
#
# By default the app calls the API at the same origin under /api, which works
# for both `npm run dev` (Vite proxy) and the single-service Railway deploy.
#
# Only set this if you deploy the frontend separately from the API:
# VITE_API_BASE_URL=https://your-api.up.railway.app
