# Minimum-version constraints: pip resolves the latest compatible release for
# whatever Python runs the install (3.12 in the Docker image, newer locally).
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
sqlalchemy>=2.0.36
psycopg[binary]>=3.2.3
pydantic[email]>=2.10.0
bcrypt>=4.2.0
pyjwt>=2.10.0
python-multipart>=0.0.20
